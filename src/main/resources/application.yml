spring:
  application:
    name: crypto-trading
  jpa:
    properties:
      hibernate:
        format_sql: true
        multiTenancy: SCHEMA
        multi_tenant_connection_provider: com.aquarius.crypto.config.tenant.SchemaMultiTenantConnectionProvider
        tenant_identifier_resolver: com.aquarius.crypto.config.tenant.ReactiveCurrentTenantResolver
    hibernate:
      ddl-auto: create
    generate-ddl: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    driverClassName: org.postgresql.Driver
    hikari:
      maximum-pool-size: 15
      auto-commit: false
  server:
    shutdown: graceful
  cache:
    type: simple

# Mismatched springframework versions.
#springdoc:
#  swagger-ui:
#    path: /swagger-ui.html
#  api-docs:
#    path: /v3/api-docs
#  info:
#    title: Crypto Trading Exchange API
#    version: v1
#    description: Production-Ready Reactive API for ETH/BTC Trading and Wallet Management.
#    contact:
#      name: Raymond
#      email: raymond@aquarius.com
#    license:
#      name: Apache 2.0
#      url: https://www.apache.org/licenses/LICENSE-2.0.html

application:
  version: 1.0.0

jwt:
  secret: ${JWT_SIGNING_SECRET:}
  expirationMs: 36000000
  refresh-expiration-ms: 86400000
  access-token-ttl-seconds: 3000 # 50 minutes
  cookie-domain: "localhost"
  cookie-path: "/"
  accessTokenTtlSeconds: 900
  refreshTokenTtlSeconds: 604800

logging:
  level:
    org:
      springframework: INFO
    com:
      aquarius:
        crypto: DEBUG


crypto:
  scheduler:
    price-aggregation-interval: 6000 # 1 minute in milliseconds